using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InHangYu : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Is HangYu");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
